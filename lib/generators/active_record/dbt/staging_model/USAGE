Description:
    Create staging model files for dbt that references the specified TABLE_NAME.

Example:
    bin/rails generate active_record:dbt:stagint_model TABLE_NAME

    This will create:
        #{export_directory_path}/stg_#{source_name}__#{table_name}.yml
